import {Role} from "../global/role";

export class User {
  username: string;
  name: string;
  vip: boolean;
  imageUrl: string;
  role: Role;
  email: string;
  bio: string;
  city: string;
  country: string;
}
